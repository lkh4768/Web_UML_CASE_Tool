MessageDecorator = draw2d.decoration.connection.ArrowDecorator.extend({

	NAME : "MessageDecorator",

	init : function(width, height) {
		this._super(width, height);
		this.setBackgroundColor(0, 0, 0);		
	}
});
/**
 * Created by kim on 2015-10-07.
 */
